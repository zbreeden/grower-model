# Contributing

- Use conventional commits: `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`.
- Keep modules self-contained under `apps/grower-ui/modules/<module-name>`.
- PRs should include a short demo note or screenshot for UI changes.
